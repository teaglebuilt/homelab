---
apiVersion: v1
kind: Service
metadata:
  name: searxng
  namespace: research
spec:
  selector:
    app: searxng
  ports:
    - port: 8080
      name: http
      protocol: TCP
      targetPort: http
