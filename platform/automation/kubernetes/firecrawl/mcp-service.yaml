---
apiVersion: v1
kind: Service
metadata:
  name: firecrawl-mcp
  namespace: automation
  labels:
    app: firecrawl-mcp
spec:
  selector:
    app: firecrawl-mcp
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: http
