---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: scraper-charleston-sales
  namespace: automation
  labels:
    app.kubernetes.io/name: scraper-charleston-sales
    app.kubernetes.io/component: scraper
spec:
  schedule: "30 6 * * *"
  timeZone: "America/New_York"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      ttlSecondsAfterFinished: 86400
      template:
        spec:
          restartPolicy: OnFailure
          nodeSelector:
            kubernetes.io/hostname: mlops-work-01
          imagePullSecrets:
            - name: ghcr-secret
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: scraper
              image: ghcr.io/nextgensolutions-llc/scrapers:latest
              imagePullPolicy: Always
              args:
                - scrapers.charleston_sales
              env:
                - name: LOOKBACK_DAYS
                  value: "2"
                - name: PAGE_SIZE
                  value: "200"
                - name: PGHOST
                  value: "postgres-rw.data.svc.cluster.local"
                - name: PGPORT
                  value: "5432"
                - name: PGDATABASE
                  value: "leads"
                - name: PGUSER
                  valueFrom:
                    secretKeyRef:
                      name: pg-scrapers-secret
                      key: username
                - name: PGPASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: pg-scrapers-secret
                      key: password
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop: ["ALL"]
                readOnlyRootFilesystem: true
              resources:
                requests:
                  cpu: 50m
                  memory: 128Mi
                limits:
                  cpu: 500m
                  memory: 256Mi
