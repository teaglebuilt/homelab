apiVersion: v1
kind: ResourceQuota
metadata:
  name: ai-resource-quotas
  namespace: ai
spec:
  hard:
    limits.cpu: "2"
    limits.memory: 2Gi
    limits.ephemeral-storage: 2Gi
    requests.cpu: "1"
    requests.memory: 1Gi
    requests.ephemeral-storage: 1Gi
