---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Shared across both clusters: the Cilium global Service that fronts the
# otel-collector. On `application` it has real backends; on `mlops` it is a
# backend-less front door that Cilium routes cross-cluster.
resources:
  - service.yaml
