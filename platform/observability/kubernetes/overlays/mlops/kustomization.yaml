apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - ../../apps/prometheus/overlays/mlops
  - ../../apps/opentelemetry/overlays/mlops
  # Loki itself runs on application; this is only the Cilium global Service so
  # mlops Vector can push logs cross-cluster via ClusterMesh.
  - ../../apps/loki/mesh-service.yaml
