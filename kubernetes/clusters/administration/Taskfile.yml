---
version: '3'

vars:
  CLUSTER: administration

tasks:
  deploy:
    cmds:
      - helmfile --kube-context={{.CLUSTER}} sync

  status:
    cmds:
      - cilium --context={{.CLUSTER}} status
      - cilium --context={{.CLUSTER}} clustermesh status
