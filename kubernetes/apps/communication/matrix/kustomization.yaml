apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - secrets.yaml
  - helmrelease.yaml
  - element-web.yaml
# Matrix OIDC/SSO configuration is handled by k8s/authentik-blueprint/matrix-provider/ (Terraform) and k8s/authentik-blueprint/matrix-secret/ (ExternalSecret)
