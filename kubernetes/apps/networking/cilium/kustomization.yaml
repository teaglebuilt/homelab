---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system
# LB-IPAM pools are per-cluster (clusters/<name>/lb-ippool.yaml), applied by the
# sync-cluster-mesh task, since both clusters share one L2 and need disjoint ranges.
resources:
  - announcement.yaml
