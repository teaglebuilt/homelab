---
apiVersion: cilium.io/v2alpha1
kind: CiliumLoadBalancerIPPool
metadata:
  name: mlops-pool
  namespace: kube-system
spec:
  blocks:
    - start: ${MLOPS_LB_POOL_START}
      stop: ${MLOPS_LB_POOL_STOP}
