---
gateway-helm:
  config:
    # backendTlsConfig:
    #   clientCertificateRef: homelab-tls
    envoyGateway:
      extensionApis:
        enableBackend: true

ai-gateway-helm:
  controller:
    enableInferenceExtension: true
