
gateway-helm:
  config:
    envoyGateway:
      gateway:
        controllerName: gateway.envoyproxy.io/gatewayclass-controller
        gatewayClassName: envoy-gateway-class
      extensionApis:
        enableBackend: true

ai-gateway-helm:
  controller:
    envoyGatewayNamespace: kube-system
