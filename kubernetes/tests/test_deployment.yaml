---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: talos-kube-api-access
spec:
  selector:
    matchLabels:
      app: talos-kube-api-access
  template:
    metadata:
      labels:
        app: talos-kube-api-access
    spec:
      containers:
        - name: talos-kube-api-access
          image: alpine:3
          command:
            - sh
            - -c
            - |
              wget -O /usr/local/bin/talosctl https://github.com/siderolabs/talos/releases/download/v1.9.2/talosctl-linux-amd64
              chmod +x /usr/local/bin/talosctl
              while true; talosctl -n 172.20.0.2 version; do sleep 1; done
