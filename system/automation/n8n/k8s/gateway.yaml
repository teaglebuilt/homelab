---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: n8n-gateway
  namespace: automation
spec:
  gatewayClassName: cilium
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same